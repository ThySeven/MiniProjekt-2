version: "1.0"
services:
  planning-service:
    image: orluff/planning-service:latest
    ports:
      - 5083:8080
    networks:
      - default
    environment:
      RabbitMQHost: rabbitmq

networks:
  default:
    driver: bridge
    name: "mitnet"
  loki:
